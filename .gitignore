.DS_Store
.claude/
node_modules/
# MLC model weights + wasm (~1GB+) — too large for normal git/GitHub; copy USB or run npm run setup-offline-llm
webllm-assets/
# vendor/mlc-ai-web-llm is small (~13MB); commit it so clones get offline WebLLM JS without npm
